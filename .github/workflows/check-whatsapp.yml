name: Check WhatsApp Status
on: workflow_dispatch
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check connection state
        run: |
          CONN=$(curl -s -H "apikey: enemeop_evo_key_2026" \
            https://enemeop-evolution.onrender.com/instance/connectionState/floricultura)
          echo "Connection: $CONN"

          echo "=== Webhook atual ==="
          WH=$(curl -s -H "apikey: enemeop_evo_key_2026" \
            https://enemeop-evolution.onrender.com/webhook/find/floricultura)
          echo "Webhook: $WH"

          echo "=== Orchestrador health ==="
          ORCH=$(curl -s https://enemeop-orchestrator.onrender.com/)
          echo "Orchestrador: $ORCH"
