name: Keep Render Services Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Orchestrator
<<<<<<< HEAD
        continue-on-error: true
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 60 https://enemeop-orchestrator.onrender.com/)
          echo "Orchestrator: $STATUS"

      - name: Ping Evolution API
        continue-on-error: true
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 60 https://enemeop-evolution.onrender.com/)
=======
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 https://enemeop-orchestrator.onrender.com/)
          echo "Orchestrator: $STATUS"

      - name: Ping Evolution API
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 https://enemeop-evolution.onrender.com/)
>>>>>>> 290c2c7d0753505a14d092d64159c5e0456fed40
          echo "Evolution API: $STATUS"
