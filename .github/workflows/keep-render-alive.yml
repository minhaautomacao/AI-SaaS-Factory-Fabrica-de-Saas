name: Keep Render Services Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Orchestrator
        continue-on-error: true
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 60 https://enemeop-orchestrator.onrender.com/)
          echo "Orchestrator: $STATUS"

      - name: Ping Evolution API
        continue-on-error: true
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 60 https://enemeop-evolution.onrender.com/)
          echo "Evolution API: $STATUS"
